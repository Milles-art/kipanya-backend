@extends('admin.layouts.app',['title'=>'Returns & Exchanges','heading'=>'Returns & Exchanges'])
@section('content')<div class="space-y-6"><div><p class="text-xs font-bold uppercase tracking-widest text-emerald-600">Kipanya Wear</p><p class="mt-2 text-sm text-gray-500">Review and process customer return and exchange requests.</p></div><form class="rounded-2xl border border-gray-200 bg-white p-4"><div class="grid gap-3 md:grid-cols-4"><input name="q" value="{{ request('q') }}" placeholder="Order, customer or ID" class="rounded-xl border border-gray-200 px-4 py-3 text-sm"><select name="status" class="rounded-xl border border-gray-200 px-4 py-3 text-sm"><option value="">All statuses</option>@foreach(['under_review','approved','rejected','received','refunded','completed'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucwords(str_replace('_',' ',$s)) }}</option>@endforeach</select><select name="request_type" class="rounded-xl border border-gray-200 px-4 py-3 text-sm"><option value="">Return + Exchange</option><option value="return" @selected(request('request_type')==='return')>Return</option><option value="exchange" @selected(request('request_type')==='exchange')>Exchange</option></select><button class="rounded-xl bg-gray-950 px-5 py-3 text-sm font-bold text-white">Filter</button></div></form><div class="overflow-hidden rounded-2xl border border-gray-200 bg-white"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-gray-50 text-xs font-bold uppercase tracking-wider text-gray-400"><tr><th class="px-5 py-3">Request</th><th class="px-5 py-3">Customer</th><th class="px-5 py-3">Order</th><th class="px-5 py-3">Type</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Date</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($requests as $r)<tr class="hover:bg-gray-50"><td class="px-5 py-4"><a class="font-bold hover:text-emerald-700" href="{{ route('admin.wear.returns.show',$r) }}">#{{ $r->id }}</a><p class="text-xs text-gray-400">{{ $r->reason }}</p></td><td class="px-5 py-4">{{ $r->user->name ?? '—' }}<p class="text-xs text-gray-400">{{ $r->user->email ?? '' }}</p></td><td class="px-5 py-4 font-semibold">{{ $r->order->order_number ?? '—' }}</td><td class="px-5 py-4">{{ ucfirst($r->request_type) }}</td><td class="px-5 py-4"><span class="rounded-full bg-gray-100 px-2.5 py-1 text-xs font-bold">{{ ucwords(str_replace('_',' ',$r->status)) }}</span></td><td class="px-5 py-4 text-gray-500">{{ $r->created_at->format('d M Y, H:i') }}</td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-gray-500">No return requests found.</td></tr>@endforelse</tbody></table></div>@if($requests->hasPages())<div class="border-t border-gray-100 px-5 py-4">{{ $requests->links() }}</div>@endif</div></div>@endsection
