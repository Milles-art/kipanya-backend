@extends('admin.layouts.app',['title'=>'Customer Enquiries','heading'=>'Customer Enquiries'])
@section('content')<div class="space-y-6"><div><p class="text-xs font-bold uppercase tracking-widest text-emerald-600">Customer service</p><p class="mt-2 text-sm text-gray-500">Manage contact and product/order enquiries from the storefront.</p></div><form class="rounded-2xl border border-gray-200 bg-white p-4 grid gap-3 md:grid-cols-4"><input name="q" value="{{ request('q') }}" placeholder="Search name, email or message" class="rounded-xl border border-gray-200 px-4 py-3 text-sm"><input name="type" value="{{ request('type') }}" placeholder="Type" class="rounded-xl border border-gray-200 px-4 py-3 text-sm"><select name="status" class="rounded-xl border border-gray-200 px-4 py-3 text-sm"><option value="">All statuses</option>@foreach(['new','read','in_progress','resolved','closed'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucwords(str_replace('_',' ',$s)) }}</option>@endforeach</select><button class="rounded-xl bg-gray-950 px-5 py-3 text-sm font-bold text-white">Filter</button></form><div class="overflow-hidden rounded-2xl border border-gray-200 bg-white"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-gray-50 text-xs font-bold uppercase tracking-wider text-gray-400"><tr><th class="px-5 py-3">Customer</th><th class="px-5 py-3">Type</th><th class="px-5 py-3">Message</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Date</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($messages as $m)<tr class="hover:bg-gray-50"><td class="px-5 py-4"><a class="font-bold hover:text-emerald-700" href="{{ route('admin.wear.enquiries.show',$m) }}">{{ $m->name }}</a><p class="text-xs text-gray-400">{{ $m->email }}</p></td><td class="px-5 py-4">{{ $m->type }}</td><td class="px-5 py-4 max-w-xl"><p class="truncate text-gray-600">{{ $m->message }}</p></td><td class="px-5 py-4"><span class="rounded-full bg-gray-100 px-2.5 py-1 text-xs font-bold">{{ ucwords(str_replace('_',' ',$m->status)) }}</span></td><td class="px-5 py-4 text-gray-500">{{ $m->created_at->format('d M Y, H:i') }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-12 text-center text-gray-500">No enquiries found.</td></tr>@endforelse</tbody></table></div>@if($messages->hasPages())<div class="border-t border-gray-100 px-5 py-4">{{ $messages->links() }}</div>@endif</div></div>@endsection
