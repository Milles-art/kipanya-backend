@extends('admin.layouts.app',['title'=>$collection->exists?'Edit Collection':'New Collection','heading'=>$collection->exists?'Edit Collection':'New Collection'])
@section('content')<form enctype="multipart/form-data" method="POST" action="{{ $collection->exists ? route('admin.wear.collections.update',$collection) : route('admin.wear.collections.store') }}" class="space-y-6">@csrf @if($collection->exists) @method('PUT') @endif<div class="grid gap-6 lg:grid-cols-2"><section class="rounded-2xl border border-gray-200 bg-white p-6 space-y-4"><div><label class="text-sm font-bold">Name</label><input name="name" value="{{ old('name',$collection->name) }}" required class="mt-2 w-full rounded-xl border border-gray-200 px-4 py-3"></div><div><label class="text-sm font-bold">Slug</label><input name="slug" value="{{ old('slug',$collection->slug) }}" class="mt-2 w-full rounded-xl border border-gray-200 px-4 py-3"></div><div><label class="text-sm font-bold">Description</label><textarea name="description" rows="5" class="mt-2 w-full rounded-xl border border-gray-200 px-4 py-3">{{ old('description',$collection->description) }}</textarea></div><div><label class="text-sm font-bold">Collection cover</label>@if($collection->cover_url)<img src="{{ $collection->cover_url }}" alt="{{ $collection->name }}" class="mt-3 h-32 w-full rounded-2xl object-cover">@endif<input type="file" name="cover_image" accept="image/jpeg,image/png,image/webp,image/avif" class="mt-2 w-full rounded-xl border border-gray-200 px-4 py-3"><p class="mt-1 text-xs text-gray-400">Up to 5 MB. A path or remote URL can also be used below.</p><input name="cover_path" value="{{ old('cover_path',$collection->cover_path) }}" class="mt-2 w-full rounded-xl border border-gray-200 px-4 py-3"></div><div class="grid grid-cols-2 gap-4"><div><label class="text-sm font-bold">Sort order</label><input type="number" min="0" name="sort_order" value="{{ old('sort_order',$collection->sort_order ?? 0) }}" class="mt-2 w-full rounded-xl border border-gray-200 px-4 py-3"></div><label class="flex items-end gap-2 pb-3 text-sm font-semibold"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$collection->exists?$collection->is_active:true))> Active</label></div></section><section class="rounded-2xl border border-gray-200 bg-white p-6"><h3 class="font-bold">Products</h3><p class="mt-1 text-sm text-gray-500">Select products and drag/order later through sort order.</p><div class="mt-4 max-h-[520px] space-y-2 overflow-y-auto">@php $selected=collect(old('product_ids',$collection->exists?$collection->products->pluck('id')->all():[]))->map(fn($id)=>(int)$id)->all(); @endphp @foreach($products as $p)<label class="flex items-center gap-3 rounded-xl border border-gray-100 p-3 hover:bg-gray-50"><input type="checkbox" name="product_ids[]" value="{{ $p->id }}" @checked(in_array($p->id,$selected,true))><span class="text-sm font-semibold">{{ $p->name }}</span></label>@endforeach</div></section></div><div class="flex justify-end gap-3"><a href="{{ route('admin.wear.collections.index') }}" class="rounded-xl border border-gray-200 px-5 py-3 text-sm font-bold">Cancel</a><button class="rounded-xl bg-gray-950 px-6 py-3 text-sm font-bold text-white">{{ $collection->exists?'Save changes':'Create collection' }}</button></div></form>@endsection
